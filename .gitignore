node_modules/
dist/
build/
coverage/
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo

# X1's Playwright rig (e2e/) keeps its own install and its own run artefacts.
e2e/test-results/
e2e/playwright-report/

# X1's run-scoped local test credentials. Generated by e2e/run.sh, reused across
# runs so a second invocation cannot rotate the project API key out from under a
# run already in flight. Local only — never commit.
e2e/.x1-secrets.env
