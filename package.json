{
  "name": "agent-wolf",
  "version": "0.0.0",
  "private": true,
  "description": "Agent Wolf — trading-hypothesis platform built on Agent Orange. See design/2026-08-20-agent-wolf.md in the agent-orange repo.",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "typecheck": "yarn workspace agent-wolf-api typecheck && yarn workspace agent-wolf-web typecheck",
    "test": "yarn workspace agent-wolf-api test && yarn workspace agent-wolf-web test"
  },
  "engines": {
    "node": ">=20"
  }
}
